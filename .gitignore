# Node.js and package managers
.next
node_modules
package-lock.json
yarn.lock

# Editor and IDE files
.idea
.vscode
*.swp
*~
*.tmp

# OS and system files
.DS_Store
Thumbs.db
*.bak
*.log

# Decap CMS specific (if needed)
.cache # Decap CMS cache directory (if it exists)
public/uploads/.cache # If you have a cache directory for uploads

# Sensitive information (never commit these)
.env
*.key
*.pem

# Deployment-related (adjust as needed)
.netlify # Netlify specific
.vercel # Vercel specific
out # Output directory

# General files that should not be committed
.history # History files
*.dump # Database dumps
*.sql # SQL files (unless for migrations)
*.lock # Lock files (except package-lock.json/yarn.lock)

# Large files (consider Git LFS for these)
*.psd # Photoshop files
*.ai # Illustrator files
*.zip # Archives
*.rar # Archives
*.gz # Compressed files
*.tar # Archives
*.dmg # Disk images
*.iso # ISO images
*.exe # Executables
*.msi # Installer packages
*.app # Application bundles

# Temporary files
*~
\#*
*.swp
.tmp*
*.temp

# Logs
*.log

# Debug files
*.dump
*.out

# Personal stuff (never commit these)
.ssh
.config

# Things you don't want in version control
.secrets
config.json # If it contains secrets
credentials.json # If it contains secrets

# If you have specific files that are generated or not needed
# add them here. Example:
report.pdf
compiled_code/

# If you are using Docker
docker-compose.override.yml

# If you are using serverless functions
.serverless

# If you are using Cypress
cypress.config.ts
cypress/